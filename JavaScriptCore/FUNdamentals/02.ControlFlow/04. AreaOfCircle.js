function CalcAreaOfCircle(radius) {
    //var radiusAsNum = Number(radius);
    var area = Math.PI * (radius*radius);
    console.log(area);
    console.log(area.toFixed(2));
}/**
 * Created by Maika on 17-Feb-17.
 */
CalcAreaOfCircle(5);